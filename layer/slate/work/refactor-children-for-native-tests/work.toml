id = "refactor-children-for-native-tests"
title = "Refactor children for native-testable core modules"
kind = "refactor"
status = "complete"
human_request = "Create a Slate for the refactor direction: gate WASM-only WIT exports, move meaningful child behavior into normal Rust modules, avoid broad dead_code allows, and restore native cargo test as a CI gate where practical."
allium_anchors = []
user_alignment = "User wants these early children to shape future child template architecture. The preferred direction is a Jon-style minimal seam: WASM-only export gates plus testable core modules before introducing separate core crates."
belief_refs = []
proof_plan = [
    '[x] folder-watch-actor gates its WIT export with #[cfg(target_arch = "wasm32")].',
    '[x] watch-null-sink gates its WIT export with #[cfg(target_arch = "wasm32")].',
    "[x] Meaningful folder-watch-actor behavior is moved behind a normal Rust core module seam with native unit tests.",
    "[x] The refactor does not introduce broad crate-level allow(dead_code) suppressions.",
    "[x] cargo test --workspace --all-targets passes locally and in CI.",
    "[x] cargo component build --release passes for both children.",
    "[x] pai-dev child check local-dev passes for both watcher children.",
]
closure_evidence = [
    'Gated `export!(FolderWatchActor)` and `export!(WatchNullSink)` behind `#[cfg(target_arch = "wasm32")]`, matching the precise Slate-style WASM-only export seam without broad native dead-code suppression.',
    "Added `children/folder-watch-actor/src/core.rs` for normal Rust behavior: stored config/stats/fingerprints, observed changes, file filtering, fingerprinting, change derivation, and JSON payload shape.",
    "Added seven native unit tests covering hidden filtering, extension filtering, first-scan suppression, created/modified/deleted derivation, JSON payload shape, and fingerprint hashing.",
    "Kept watch-null-sink intentionally thin; it only needed the WASM export gate to avoid native test linker issues.",
    "Updated CI workflows to use `cargo test --workspace --all-targets` as the Tier 2 gate while retaining release component builds as Tier 3.",
    "Verified `cargo fmt --all --check`.",
    "Verified `cargo test --workspace --all-targets` with 7 passing folder-watch-core tests.",
    "Verified `cargo clippy --workspace --all-targets -- -D warnings`.",
    "Verified release component builds for both watcher children.",
    "Verified pai-dev local-dev diagnostics pass for both watcher children; diagnostics still report non-fatal unknown contract/runtime type warnings.",
]
blocked_by = []
implementation_plan = [
    "[x] First apply the surgical Slate-style fix: gate export!(...) in both child crates to wasm32 only.",
    "[x] Run native cargo test to see the remaining failures before designing more structure.",
    "[x] Extract folder-watch-actor's pure-ish diff/filter/change payload logic into src/core.rs with focused unit tests.",
    "[x] Keep watch-null-sink simple; only extract a tiny helper/module if needed for meaningful native testing.",
    "[x] Update CI from cargo check to cargo test once native tests pass without broad dead_code suppression.",
    "[x] Keep component release builds and pai-dev diagnostics as the WIT/runtime authority gates.",
    "[x] Record any child-template doctrine learned from the refactor in project docs or Slate closure evidence.",
    "[x] Decide in closure whether this should become a reusable belief/template rule; do not force an Allium anchor for this architecture-only refactor.",
]
belief_harvest_decision = "candidate belief: Patina child templates should gate WIT/component exports to wasm32 and keep meaningful behavior in normal Rust modules tested natively before reaching for separate core crates. Capture after user review if desired."
created_at = "1779326871"
updated_at = "1779360231"
closed_at = "1779360231"
